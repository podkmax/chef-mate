version: "3.8"
services:
  db:
    image: postgres:15.3
    restart: always
    environment:
      POSTGRES_USER: chefmate
      POSTGRES_PASSWORD: chefmate
      POSTGRES_DB: chefmate
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
